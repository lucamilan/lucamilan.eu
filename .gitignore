# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream
# Archival directory: superseded and retired files
.old/
# Claude Code: local-only guidance and skills, not for the public repo
.claude/
# Public repo minimum: only index.html and its referenced assets are committed.
# CV source material (markdown and exports) is kept locally only, not published.
*.md
!README.md
*.pdf
*.docx
